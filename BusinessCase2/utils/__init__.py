"""
BusinessCase2 model utilities.

Each submodule is a self-contained script for one model family.
Run any script directly::

    python -m utils.linear_reg
    python -m utils.rand_forest
    # etc.

All scripts save results to ``data/pickled_files/<model_name>/``.
"""
